--[[
    Nexzan Hub Premium UI Library
    Theme: Modern Premium Dark Glass
    Target: Mobile & PC (Responsive)
    Version: 2.0.0
]]

local NexzanUI = {}

local TweenService = game:GetService("TweenService")
local MarketplaceService = game:GetService("MarketplaceService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer

-- Palette
local Colors = {
    MainBg = Color3.fromRGB(22, 22, 22),
    HeaderBg = Color3.fromRGB(20, 20, 20),
    SidebarBg = Color3.fromRGB(27, 27, 27),
    CardBg = Color3.fromRGB(39, 39, 39),
    SearchBg = Color3.fromRGB(44, 44, 44),
    Border = Color3.fromRGB(60, 60, 60),
    TextWhite = Color3.fromRGB(245, 245, 245),
    TextGray = Color3.fromRGB(170, 170, 170),
    BadgeBlue = Color3.fromRGB(50, 110, 255),
    ToggleOff = Color3.fromRGB(85, 85, 85),
    ToggleOn = Color3.fromRGB(0, 255, 120),
    GlowGreen = Color3.fromRGB(0, 255, 120)
}

-- Utility: Dragging
local function makeDraggable(frame, dragHandle)
    local dragging, dragInput, dragStart, startPos
    dragHandle = dragHandle or frame

    dragHandle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    dragHandle.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

-- Create Ripple Effect (Visual Touch)
local function createRipple(button)
    local ripple = Instance.new("Frame")
    ripple.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    ripple.BackgroundTransparency = 0.8
    ripple.BorderSizePixel = 0
    ripple.Size = UDim2.new(0, 0, 0, 0)
    ripple.Position = UDim2.new(0.5, 0, 0.5, 0)
    ripple.AnchorPoint = Vector2.new(0.5, 0.5)
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = ripple
    ripple.Parent = button
    
    TweenService:Create(ripple, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
        Size = UDim2.new(2, 0, 2, 0),
        BackgroundTransparency = 1
    }):Play()
    
    game.Debris:AddItem(ripple, 0.4)
end

function NexzanUI:MakeWindow(config)
    config = config or {}
    local windowName = config.Name or "Nexzan Hub"
    local windowDesc = config.Description or "Made Nexzan"
    local version = config.Version or "2.0.0"
    
    local gameName = "Unknown Game"
    pcall(function()
        gameName = MarketplaceService:GetProductInfo(game.PlaceId).Name
    end)

    -- ScreenGui Setup
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "NexzanHub_Premium_UI"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    if syn and syn.protect_gui then
        syn.protect_gui(ScreenGui)
        ScreenGui.Parent = CoreGui
    elseif gethui then
        ScreenGui.Parent = gethui()
    else
        ScreenGui.Parent = player:WaitForChild("PlayerGui")
    end

    -- Watermark
    local Watermark = Instance.new("Frame")
    Watermark.Name = "Watermark"
    Watermark.Size = UDim2.new(0, 130, 0, 30)
    Watermark.Position = UDim2.new(0, 15, 0, 15)
    Watermark.BackgroundColor3 = Colors.MainBg
    Watermark.Parent = ScreenGui
    Instance.new("UICorner", Watermark).CornerRadius = UDim.new(0, 6)
    local wmStroke = Instance.new("UIStroke", Watermark)
    wmStroke.Color = Colors.Border
    wmStroke.Thickness = 1
    
    local wmLabel = Instance.new("TextLabel")
    wmLabel.Size = UDim2.new(1, 0, 1, 0)
    wmLabel.BackgroundTransparency = 1
    wmLabel.Text = windowName
    wmLabel.TextColor3 = Colors.TextWhite
    wmLabel.Font = Enum.Font.GothamBold
    wmLabel.TextSize = 13
    wmLabel.Parent = Watermark
    makeDraggable(Watermark)

    -- Main Window
    local MainWindow = Instance.new("Frame")
    MainWindow.Name = "MainWindow"
    MainWindow.Size = UDim2.new(0, 600, 0, 360)
    MainWindow.Position = UDim2.new(0.5, -300, 0.5, -180)
    MainWindow.BackgroundColor3 = Colors.MainBg
    MainWindow.ClipsDescendants = false
    MainWindow.Parent = ScreenGui
    Instance.new("UICorner", MainWindow).CornerRadius = UDim.new(0, 12)
    local mainStroke = Instance.new("UIStroke", MainWindow)
    mainStroke.Color = Colors.Border
    mainStroke.Thickness = 1

    local mainShadow = Instance.new("ImageLabel")
    mainShadow.Name = "Shadow"
    mainShadow.Position = UDim2.new(0, -15, 0, -15)
    mainShadow.Size = UDim2.new(1, 30, 1, 30)
    mainShadow.BackgroundTransparency = 1
    mainShadow.Image = "rbxassetid://1316045217"
    mainShadow.ImageColor3 = Color3.fromRGB(0, 0, 0)
    mainShadow.ImageTransparency = 0.6
    mainShadow.ScaleType = Enum.ScaleType.Slice
    mainShadow.SliceCenter = Rect.new(10, 10, 118, 118)
    mainShadow.ZIndex = 0
    mainShadow.Parent = MainWindow

    -- Header
    local Header = Instance.new("Frame")
    Header.Size = UDim2.new(1, 0, 0, 55)
    Header.BackgroundColor3 = Colors.HeaderBg
    Header.BorderSizePixel = 0
    Header.Parent = MainWindow
    Instance.new("UICorner", Header).CornerRadius = UDim.new(0, 12)
    local headerCover = Instance.new("Frame")
    headerCover.Size = UDim2.new(1, 0, 0, 10)
    headerCover.Position = UDim2.new(0, 0, 1, -10)
    headerCover.BackgroundColor3 = Colors.HeaderBg
    headerCover.BorderSizePixel = 0
    headerCover.Parent = Header
    makeDraggable(MainWindow, Header)

    local LogoContainer = Instance.new("Frame")
    LogoContainer.Size = UDim2.new(0, 36, 0, 36)
    LogoContainer.Position = UDim2.new(0, 12, 0, 9)
    LogoContainer.BackgroundTransparency = 1
    LogoContainer.Parent = Header
    local LogoImg = Instance.new("ImageLabel")
    LogoImg.Size = UDim2.new(1, 0, 1, 0)
    LogoImg.BackgroundTransparency = 1
    LogoImg.Image = "rbxassetid://94703576073885"
    LogoImg.Parent = LogoContainer
    Instance.new("UICorner", LogoImg).CornerRadius = UDim.new(1, 0)
    local logoStroke = Instance.new("UIStroke", LogoImg)
    logoStroke.Color = Colors.GlowGreen
    logoStroke.Thickness = 1.5

    local TitleContainer = Instance.new("Frame")
    TitleContainer.Position = UDim2.new(0, 60, 0, 8)
    TitleContainer.Size = UDim2.new(0, 200, 0, 40)
    TitleContainer.BackgroundTransparency = 1
    TitleContainer.Parent = Header
    local MapLabel = Instance.new("TextLabel")
    MapLabel.Size = UDim2.new(1, 0, 0, 20)
    MapLabel.BackgroundTransparency = 1
    MapLabel.Text = gameName
    MapLabel.TextColor3 = Colors.TextWhite
    MapLabel.Font = Enum.Font.GothamBold
    MapLabel.TextSize = 14
    MapLabel.TextXAlignment = Enum.TextXAlignment.Left
    MapLabel.Parent = TitleContainer
    local AuthorLabel = Instance.new("TextLabel")
    AuthorLabel.Size = UDim2.new(1, 0, 0, 16)
    AuthorLabel.Position = UDim2.new(0, 0, 0, 18)
    AuthorLabel.BackgroundTransparency = 1
    AuthorLabel.Text = windowDesc
    AuthorLabel.TextColor3 = Colors.TextGray
    AuthorLabel.Font = Enum.Font.GothamSemiBold
    AuthorLabel.TextSize = 11
    AuthorLabel.TextXAlignment = Enum.TextXAlignment.Left
    AuthorLabel.Parent = TitleContainer

    local Badge = Instance.new("Frame")
    Badge.Position = UDim2.new(0, 250, 0, 16)
    Badge.Size = UDim2.new(0, 130, 0, 22)
    Badge.BackgroundColor3 = Colors.BadgeBlue
    Badge.Parent = Header
    Instance.new("UICorner", Badge).CornerRadius = UDim.new(1, 0)
    local badgeLabel = Instance.new("TextLabel")
    badgeLabel.Size = UDim2.new(1, 0, 1, 0)
    badgeLabel.BackgroundTransparency = 1
    badgeLabel.Text = "New Version | " .. version
    badgeLabel.TextColor3 = Colors.TextWhite
    badgeLabel.Font = Enum.Font.GothamBold
    badgeLabel.TextSize = 11
    badgeLabel.Parent = Badge

    -- Sidebar
    local Sidebar = Instance.new("Frame")
    Sidebar.Size = UDim2.new(0, 180, 1, -55)
    Sidebar.Position = UDim2.new(0, 0, 0, 55)
    Sidebar.BackgroundColor3 = Colors.SidebarBg
    Sidebar.BorderSizePixel = 0
    Sidebar.Parent = MainWindow
    Instance.new("UICorner", Sidebar).CornerRadius = UDim.new(0, 12)
    local sidebarCover = Instance.new("Frame")
    sidebarCover.Size = UDim2.new(0, 12, 1, 0)
    sidebarCover.Position = UDim2.new(1, -12, 0, 0)
    sidebarCover.BackgroundColor3 = Colors.SidebarBg
    sidebarCover.BorderSizePixel = 0
    sidebarCover.Parent = Sidebar
    local sidebarTopCover = Instance.new("Frame")
    sidebarTopCover.Size = UDim2.new(1, 0, 0, 12)
    sidebarTopCover.BackgroundColor3 = Colors.SidebarBg
    sidebarTopCover.BorderSizePixel = 0
    sidebarTopCover.Parent = Sidebar

    local SearchContainer = Instance.new("Frame")
    SearchContainer.Size = UDim2.new(1, -20, 0, 32)
    SearchContainer.Position = UDim2.new(0, 10, 0, 10)
    SearchContainer.BackgroundColor3 = Colors.SearchBg
    SearchContainer.Parent = Sidebar
    Instance.new("UICorner", SearchContainer).CornerRadius = UDim.new(0, 8)
    local searchStroke = Instance.new("UIStroke", SearchContainer)
    searchStroke.Color = Colors.Border
    searchStroke.Thickness = 1

    local SearchBox = Instance.new("TextBox")
    SearchBox.Size = UDim2.new(1, -10, 1, 0)
    SearchBox.Position = UDim2.new(0, 10, 0, 0)
    SearchBox.BackgroundTransparency = 1
    SearchBox.Text = ""
    SearchBox.PlaceholderText = "🔍 Search feature..."
    SearchBox.PlaceholderColor3 = Colors.TextGray
    SearchBox.TextColor3 = Colors.TextWhite
    SearchBox.Font = Enum.Font.Gotham
    SearchBox.TextSize = 12
    SearchBox.TextXAlignment = Enum.TextXAlignment.Left
    SearchBox.Parent = SearchContainer

    local TabScroll = Instance.new("ScrollingFrame")
    TabScroll.Size = UDim2.new(1, 0, 1, -52)
    TabScroll.Position = UDim2.new(0, 0, 0, 52)
    TabScroll.BackgroundTransparency = 1
    TabScroll.BorderSizePixel = 0
    TabScroll.ScrollBarThickness = 2
    TabScroll.ScrollBarImageColor3 = Colors.Border
    TabScroll.AutomaticCanvasSize = Enum.AutomaticCanvasSize.Y
    TabScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    TabScroll.Parent = Sidebar
    local tabPadding = Instance.new("UIPadding")
    tabPadding.PaddingLeft = UDim.new(0, 10)
    tabPadding.PaddingRight = UDim.new(0, 10)
    tabPadding.PaddingTop = UDim.new(0, 5)
    tabPadding.PaddingBottom = UDim.new(0, 5)
    tabPadding.Parent = TabScroll
    local tabLayout = Instance.new("UIListLayout")
    tabLayout.SortOrder = Enum.SortOrder.LayoutOrder
    tabLayout.Padding = UDim.new(0, 6)
    tabLayout.Parent = TabScroll

    local FeatureArea = Instance.new("Frame")
    FeatureArea.Size = UDim2.new(1, -180, 1, -55)
    FeatureArea.Position = UDim2.new(0, 180, 0, 55)
    FeatureArea.BackgroundTransparency = 1
    FeatureArea.Parent = MainWindow

    -- Floating Window
    local FloatingWindow = Instance.new("TextButton")
    FloatingWindow.Size = UDim2.new(0, 160, 0, 34)
    FloatingWindow.Position = UDim2.new(0.5, -80, 0.1, 0)
    FloatingWindow.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
    FloatingWindow.BackgroundTransparency = 0.2
    FloatingWindow.Text = ""
    FloatingWindow.AutoButtonColor = false
    FloatingWindow.Visible = false
    FloatingWindow.Parent = ScreenGui
    Instance.new("UICorner", FloatingWindow).CornerRadius = UDim.new(0, 8)
    local floatStroke = Instance.new("UIStroke", FloatingWindow)
    floatStroke.Color = Colors.GlowGreen
    floatStroke.Thickness = 1.5
    local DragIcon = Instance.new("TextLabel")
    DragIcon.Size = UDim2.new(0, 24, 1, 0)
    DragIcon.Position = UDim2.new(0, 8, 0, 0)
    DragIcon.BackgroundTransparency = 1
    DragIcon.Text = "☰"
    DragIcon.TextColor3 = Colors.TextWhite
    DragIcon.Font = Enum.Font.GothamBold
    DragIcon.TextSize = 14
    DragIcon.Parent = FloatingWindow
    local MoonIcon = Instance.new("TextLabel")
    MoonIcon.Size = UDim2.new(0, 20, 1, 0)
    MoonIcon.Position = UDim2.new(0, 32, 0, 0)
    MoonIcon.BackgroundTransparency = 1
    MoonIcon.Text = "🌙"
    MoonIcon.TextSize = 12
    MoonIcon.Parent = FloatingWindow
    local FloatTitle = Instance.new("TextLabel")
    FloatTitle.Size = UDim2.new(1, -60, 1, 0)
    FloatTitle.Position = UDim2.new(0, 55, 0, 0)
    FloatTitle.BackgroundTransparency = 1
    FloatTitle.Text = windowName
    FloatTitle.TextColor3 = Colors.ToggleOn
    FloatTitle.Font = Enum.Font.GothamBold
    FloatTitle.TextSize = 13
    FloatTitle.TextXAlignment = Enum.TextXAlignment.Left
    FloatTitle.Parent = FloatingWindow
    makeDraggable(FloatingWindow)

    -- Window Controls
    local isMinimized = false
    local function minimizeUI()
        if isMinimized then return end
        isMinimized = true
        TweenService:Create(MainWindow, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.new(0, 600, 0, 0), BackgroundTransparency = 1}):Play()
        task.wait(0.2)
        MainWindow.Visible = false
        FloatingWindow.Visible = true
        FloatingWindow.Size = UDim2.new(0, 0, 0, 34)
        TweenService:Create(FloatingWindow, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 160, 0, 34)}):Play()
    end

    local function maximizeUI()
        if not isMinimized then return end
        isMinimized = false
        FloatingWindow.Visible = false
        MainWindow.Visible = true
        MainWindow.Size = UDim2.new(0, 600, 0, 0)
        TweenService:Create(MainWindow, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 600, 0, 360), BackgroundTransparency = 0}):Play()
    end
    FloatingWindow.MouseButton1Click:Connect(maximizeUI)

    local Controls = Instance.new("Frame")
    Controls.Size = UDim2.new(0, 100, 1, 0)
    Controls.Position = UDim2.new(1, -110, 0, 0)
    Controls.BackgroundTransparency = 1
    Controls.Parent = Header
    local controlLayout = Instance.new("UIListLayout")
    controlLayout.FillDirection = Enum.FillDirection.Horizontal
    controlLayout.HorizontalAlignment = Enum.HorizontalAlignment.Right
    controlLayout.VerticalAlignment = Enum.VerticalAlignment.Center
    controlLayout.Padding = UDim.new(0, 8)
    controlLayout.Parent = Controls

    local function createHeaderBtn(text, color, onClick)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, 24, 0, 24)
        btn.BackgroundColor3 = Colors.CardBg
        btn.Text = text
        btn.TextColor3 = color
        btn.Font = Enum.Font.GothamBold
        btn.TextSize = 14
        btn.AutoButtonColor = false
        btn.Parent = Controls
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
        local stroke = Instance.new("UIStroke", btn)
        stroke.Color = Colors.Border
        stroke.Thickness = 1
        btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Colors.SearchBg}):Play() end)
        btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.2), {BackgroundColor3 = Colors.CardBg}):Play() end)
        btn.MouseButton1Click:Connect(onClick)
    end
    createHeaderBtn("-", Colors.TextWhite, minimizeUI)
    createHeaderBtn("□", Colors.TextWhite, function() end)
    createHeaderBtn("×", Color3.fromRGB(255, 70, 70), function() ScreenGui:Destroy() end)

    -- Opening Animation
    MainWindow.Size = UDim2.new(0, 600, 0, 0)
    TweenService:Create(MainWindow, TweenInfo.new(0.35, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Size = UDim2.new(0, 600, 0, 360)}):Play()

    -- Library System Variables
    local Tabs = {}
    local TabPages = {}
    local FirstTab = true
    local ActiveTab = nil
    local AllElements = {}

    SearchBox.Changed:Connect(function(property)
        if property == "Text" then
            local searchStr = SearchBox.Text:lower()
            for _, elementData in ipairs(AllElements) do
                if elementData.tab == ActiveTab then
                    if searchStr == "" or elementData.name:lower():find(searchStr) then
                        elementData.frame.Visible = true
                    else
                        elementData.frame.Visible = false
                    end
                end
            end
        end
    end)

    local WindowLibrary = {}

    function WindowLibrary:MakeTab(tabConfig)
        local tabName = tabConfig.Name or "Tab"
        local icon = tabConfig.Icon

        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 36)
        btn.BackgroundTransparency = 1
        btn.BackgroundColor3 = Colors.CardBg
        btn.Text = "  " .. tabName
        btn.TextColor3 = Colors.TextGray
        btn.Font = Enum.Font.GothamSemiBold
        btn.TextSize = 12
        btn.TextXAlignment = Enum.TextXAlignment.Left
        btn.AutoButtonColor = false
        btn.Parent = TabScroll
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

        local page = Instance.new("ScrollingFrame")
        page.Size = UDim2.new(1, 0, 1, 0)
        page.BackgroundTransparency = 1
        page.BorderSizePixel = 0
        page.ScrollBarThickness = 4
        page.ScrollBarImageColor3 = Colors.Border
        page.AutomaticCanvasSize = Enum.AutomaticCanvasSize.Y
        page.CanvasSize = UDim2.new(0, 0, 0, 0)
        page.Visible = false
        page.Parent = FeatureArea
        
        local pagePadding = Instance.new("UIPadding")
        pagePadding.PaddingLeft = UDim.new(0, 15)
        pagePadding.PaddingRight = UDim.new(0, 15)
        pagePadding.PaddingTop = UDim.new(0, 15)
        pagePadding.PaddingBottom = UDim.new(0, 15)
        pagePadding.Parent = page
        local pageLayout = Instance.new("UIListLayout")
        pageLayout.SortOrder = Enum.SortOrder.LayoutOrder
        pageLayout.Padding = UDim.new(0, 10)
        pageLayout.Parent = page

        table.insert(Tabs, {Btn = btn, Page = page, Name = tabName})

        btn.MouseButton1Click:Connect(function()
            for _, t in ipairs(Tabs) do
                t.Page.Visible = (t.Name == tabName)
                if t.Name == tabName then
                    TweenService:Create(t.Btn, TweenInfo.new(0.25), {BackgroundColor3 = Colors.CardBg, TextColor3 = Colors.TextWhite}):Play()
                    ActiveTab = tabName
                else
                    TweenService:Create(t.Btn, TweenInfo.new(0.25), {BackgroundColor3 = Color3.fromRGB(0,0,0), BackgroundTransparency = 1, TextColor3 = Colors.TextGray}):Play()
                end
            end
            SearchBox.Text = "" -- Reset search on tab switch
        end)

        if FirstTab then
            FirstTab = false
            page.Visible = true
            ActiveTab = tabName
            TweenService:Create(btn, TweenInfo.new(0), {BackgroundColor3 = Colors.CardBg, TextColor3 = Colors.TextWhite}):Play()
        end

        local TabLibrary = {}

        local function createCardFrame(titleText, descText, sizeY)
            local Card = Instance.new("Frame")
            Card.Size = UDim2.new(1, 0, 0, sizeY or 60)
            Card.BackgroundColor3 = Colors.CardBg
            Card.Parent = page
            Instance.new("UICorner", Card).CornerRadius = UDim.new(0, 10)
            local cardStroke = Instance.new("UIStroke", Card)
            cardStroke.Color = Colors.Border
            cardStroke.Thickness = 1

            local Title = Instance.new("TextLabel")
            Title.Size = UDim2.new(1, -70, 0, 22)
            Title.Position = UDim2.new(0, 15, 0, 10)
            Title.BackgroundTransparency = 1
            Title.Text = titleText
            Title.TextColor3 = Colors.TextWhite
            Title.Font = Enum.Font.GothamBold
            Title.TextSize = 13
            Title.TextXAlignment = Enum.TextXAlignment.Left
            Title.Parent = Card

            if descText then
                local Desc = Instance.new("TextLabel")
                Desc.Size = UDim2.new(1, -70, 0, 18)
                Desc.Position = UDim2.new(0, 15, 0, 30)
                Desc.BackgroundTransparency = 1
                Desc.Text = descText
                Desc.TextColor3 = Colors.TextGray
                Desc.Font = Enum.Font.Gotham
                Desc.TextSize = 11
                Desc.TextXAlignment = Enum.TextXAlignment.Left
                Desc.Parent = Card
            else
                Title.Position = UDim2.new(0, 15, 0, 0)
                Title.Size = UDim2.new(1, -70, 1, 0)
            end

            table.insert(AllElements, {frame = Card, name = titleText, tab = tabName})
            return Card
        end

        function TabLibrary:AddToggle(opt)
            local state = opt.Default or false
            local cb = opt.Callback or function() end
            local Card = createCardFrame(opt.Name, opt.Description)

            local ToggleBg = Instance.new("TextButton")
            ToggleBg.Size = UDim2.new(0, 44, 0, 24)
            ToggleBg.Position = UDim2.new(1, -59, 0.5, -12)
            ToggleBg.BackgroundColor3 = state and Colors.ToggleOn or Colors.ToggleOff
            ToggleBg.Text = ""
            ToggleBg.AutoButtonColor = false
            ToggleBg.Parent = Card
            Instance.new("UICorner", ToggleBg).CornerRadius = UDim.new(1, 0)

            local ToggleBall = Instance.new("Frame")
            ToggleBall.Size = UDim2.new(0, 18, 0, 18)
            ToggleBall.Position = state and UDim2.new(1, -21, 0.5, -9) or UDim2.new(0, 3, 0.5, -9)
            ToggleBall.BackgroundColor3 = Colors.TextWhite
            ToggleBall.Parent = ToggleBg
            Instance.new("UICorner", ToggleBall).CornerRadius = UDim.new(1, 0)

            ToggleBg.MouseButton1Click:Connect(function()
                state = not state
                local targetPos = state and UDim2.new(1, -21, 0.5, -9) or UDim2.new(0, 3, 0.5, -9)
                local targetColor = state and Colors.ToggleOn or Colors.ToggleOff
                TweenService:Create(ToggleBall, TweenInfo.new(0.2), {Position = targetPos}):Play()
                TweenService:Create(ToggleBg, TweenInfo.new(0.2), {BackgroundColor3 = targetColor}):Play()
                task.spawn(cb, state)
            end)
        end

        function TabLibrary:AddButton(opt)
            local cb = opt.Callback or function() end
            local Card = createCardFrame(opt.Name, opt.Description)
            
            local Btn = Instance.new("TextButton")
            Btn.Size = UDim2.new(0, 34, 0, 34)
            Btn.Position = UDim2.new(1, -49, 0.5, -17)
            Btn.BackgroundColor3 = Colors.SearchBg
            Btn.Text = "👆"
            Btn.TextColor3 = Colors.TextWhite
            Btn.Font = Enum.Font.GothamBold
            Btn.TextSize = 14
            Btn.ClipsDescendants = true
            Btn.AutoButtonColor = false
            Btn.Parent = Card
            Instance.new("UICorner", Btn).CornerRadius = UDim.new(0, 8)
            
            local stroke = Instance.new("UIStroke", Btn)
            stroke.Color = Colors.Border
            stroke.Thickness = 1

            Btn.MouseButton1Click:Connect(function()
                createRipple(Btn)
                task.spawn(cb)
            end)
        end

        function TabLibrary:AddSlider(opt)
            local min = opt.Min or 0
            local max = opt.Max or 100
            local default = opt.Default or min
            local cb = opt.Callback or function() end
            local Card = createCardFrame(opt.Name, opt.Description, 75)

            local ValueLabel = Instance.new("TextLabel")
            ValueLabel.Size = UDim2.new(0, 50, 0, 20)
            ValueLabel.Position = UDim2.new(1, -65, 0, 10)
            ValueLabel.BackgroundTransparency = 1
            ValueLabel.Text = tostring(default)
            ValueLabel.TextColor3 = Colors.TextWhite
            ValueLabel.Font = Enum.Font.GothamBold
            ValueLabel.TextSize = 12
            ValueLabel.TextXAlignment = Enum.TextXAlignment.Right
            ValueLabel.Parent = Card

            local SliderBg = Instance.new("Frame")
            SliderBg.Size = UDim2.new(1, -30, 0, 6)
            SliderBg.Position = UDim2.new(0, 15, 1, -20)
            SliderBg.BackgroundColor3 = Colors.SearchBg
            SliderBg.Parent = Card
            Instance.new("UICorner", SliderBg).CornerRadius = UDim.new(1, 0)

            local SliderFill = Instance.new("Frame")
            local percentage = math.clamp((default - min) / (max - min), 0, 1)
            SliderFill.Size = UDim2.new(percentage, 0, 1, 0)
            SliderFill.BackgroundColor3 = Colors.BadgeBlue
            SliderFill.Parent = SliderBg
            Instance.new("UICorner", SliderFill).CornerRadius = UDim.new(1, 0)

            local DragBtn = Instance.new("TextButton")
            DragBtn.Size = UDim2.new(1, 0, 1, 20)
            DragBtn.Position = UDim2.new(0, 0, 0, -10)
            DragBtn.BackgroundTransparency = 1
            DragBtn.Text = ""
            DragBtn.Parent = SliderBg

            local isDragging = false
            local function updateSlider(input)
                local pos = math.clamp((input.Position.X - SliderBg.AbsolutePosition.X) / SliderBg.AbsoluteSize.X, 0, 1)
                local val = math.floor(min + ((max - min) * pos))
                TweenService:Create(SliderFill, TweenInfo.new(0.1), {Size = UDim2.new(pos, 0, 1, 0)}):Play()
                ValueLabel.Text = tostring(val)
                task.spawn(cb, val)
            end

            DragBtn.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    isDragging = true
                    updateSlider(input)
                end
            end)
            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    isDragging = false
                end
            end)
            UserInputService.InputChanged:Connect(function(input)
                if isDragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    updateSlider(input)
                end
            end)
        end

        function TabLibrary:AddDropdown(opt)
            local default = opt.Default or opt.Options[1]
            local cb = opt.Callback or function() end
            local Card = createCardFrame(opt.Name, opt.Description)
            Card.ClipsDescendants = true

            local DropBtn = Instance.new("TextButton")
            DropBtn.Size = UDim2.new(0, 120, 0, 30)
            DropBtn.Position = UDim2.new(1, -135, 0, 15)
            DropBtn.BackgroundColor3 = Colors.SearchBg
            DropBtn.Text = "  " .. tostring(default) .. " ▼"
            DropBtn.TextColor3 = Colors.TextWhite
            DropBtn.Font = Enum.Font.Gotham
            DropBtn.TextSize = 12
            DropBtn.TextXAlignment = Enum.TextXAlignment.Left
            DropBtn.AutoButtonColor = false
            DropBtn.Parent = Card
            Instance.new("UICorner", DropBtn).CornerRadius = UDim.new(0, 6)
            local dropStroke = Instance.new("UIStroke", DropBtn)
            dropStroke.Color = Colors.Border
            dropStroke.Thickness = 1

            local ListFrame = Instance.new("ScrollingFrame")
            ListFrame.Size = UDim2.new(1, -30, 0, 100)
            ListFrame.Position = UDim2.new(0, 15, 0, 60)
            ListFrame.BackgroundTransparency = 1
            ListFrame.BorderSizePixel = 0
            ListFrame.ScrollBarThickness = 2
            ListFrame.Visible = false
            ListFrame.Parent = Card
            local listLayout = Instance.new("UIListLayout", ListFrame)
            listLayout.Padding = UDim.new(0, 5)

            local isDropped = false
            DropBtn.MouseButton1Click:Connect(function()
                isDropped = not isDropped
                if isDropped then
                    ListFrame.Visible = true
                    TweenService:Create(Card, TweenInfo.new(0.3), {Size = UDim2.new(1, 0, 0, 170)}):Play()
                else
                    TweenService:Create(Card, TweenInfo.new(0.3), {Size = UDim2.new(1, 0, 0, 60)}):Play()
                    task.wait(0.3)
                    ListFrame.Visible = false
                end
            end)

            for _, option in ipairs(opt.Options) do
                local optBtn = Instance.new("TextButton")
                optBtn.Size = UDim2.new(1, -10, 0, 25)
                optBtn.BackgroundColor3 = Colors.MainBg
                optBtn.Text = tostring(option)
                optBtn.TextColor3 = Colors.TextGray
                optBtn.Font = Enum.Font.Gotham
                optBtn.TextSize = 11
                optBtn.Parent = ListFrame
                Instance.new("UICorner", optBtn).CornerRadius = UDim.new(0, 4)

                optBtn.MouseButton1Click:Connect(function()
                    DropBtn.Text = "  " .. tostring(option) .. " ▼"
                    isDropped = false
                    TweenService:Create(Card, TweenInfo.new(0.3), {Size = UDim2.new(1, 0, 0, 60)}):Play()
                    task.wait(0.3)
                    ListFrame.Visible = false
                    task.spawn(cb, option)
                end)
            end
        end
        
        function TabLibrary:AddTextbox(opt)
            local cb = opt.Callback or function() end
            local Card = createCardFrame(opt.Name, opt.Description)
            
            local BoxContainer = Instance.new("Frame")
            BoxContainer.Size = UDim2.new(0, 150, 0, 30)
            BoxContainer.Position = UDim2.new(1, -165, 0, 15)
            BoxContainer.BackgroundColor3 = Colors.SearchBg
            BoxContainer.Parent = Card
            Instance.new("UICorner", BoxContainer).CornerRadius = UDim.new(0, 6)
            local boxStroke = Instance.new("UIStroke", BoxContainer)
            boxStroke.Color = Colors.Border
            boxStroke.Thickness = 1
            
            local TxtBox = Instance.new("TextBox")
            TxtBox.Size = UDim2.new(1, -10, 1, 0)
            TxtBox.Position = UDim2.new(0, 5, 0, 0)
            TxtBox.BackgroundTransparency = 1
            TxtBox.Text = ""
            TxtBox.PlaceholderText = opt.Placeholder or "Enter text..."
            TxtBox.TextColor3 = Colors.TextWhite
            TxtBox.Font = Enum.Font.Gotham
            TxtBox.TextSize = 12
            TxtBox.ClearTextOnFocus = false
            TxtBox.Parent = BoxContainer
            
            TxtBox.FocusLost:Connect(function()
                task.spawn(cb, TxtBox.Text)
            end)
        end
        
        function TabLibrary:AddLabel(opt)
            createCardFrame(opt.Name, nil, 40)
        end

        return TabLibrary
    end

    return WindowLibrary
end

return NexzanUI
